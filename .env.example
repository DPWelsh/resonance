# Discogs personal access token — get one at https://www.discogs.com/settings/developers
# (Generate token -> paste below). Free, instant, read-only is fine.
Discogs_Token=your_discogs_personal_access_token_here
